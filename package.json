{
  "name": "pug-checker",
  "version": "0.8.0",
  "description": "Lokaler PlantUML Live-Editor mit Split-View und Fehleranzeige",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "docker:up": "BUILD_SHA=$(git rev-parse --short HEAD 2>/dev/null || echo dev) BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ) docker compose up -d --build"
  },
  "dependencies": {
    "plantuml-encoder": "^1.4.0"
  }
}
